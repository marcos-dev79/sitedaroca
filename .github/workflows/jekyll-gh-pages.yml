# Workflow antigo (JamesIves / branch gh-pages).
# Desativado para não competir com deploy.yml (GitHub Pages oficial).
# Use o workflow "Deploy site" em .github/workflows/deploy.yml.

name: Deploy Jekyll (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/deploy.yml"
